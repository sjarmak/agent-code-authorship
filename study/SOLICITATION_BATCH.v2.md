# Frozen Contemporary Human-Attestation Solicitation Batch

This batch was selected and screened without consulting classifier outputs or
source-feature values. It is derived from
`reference-candidates.v2.json` (SHA-256
`8591ebac1c0ac17bac883829e2e02c5b7fb8b4248be86c50308c3e6bbe4769ed`)
and the two pinned mechanical audit batches.

Passing the screen means only that the repository contains at least 2,000
surviving eligible Python or Go lines introduced from 2024-01-01 through
2026-07-24. It is not an authorship label. No contact has been made.

## Contact order

The contact order is the original frozen `selection_key` order within each
language. Contact attempts and responses must be written to the separate
attestation ledger; this file and the candidate frame remain immutable.

### Python

1. `chinesehuazhou/python-weekly`
2. `timbrel/GitSavvy`
3. `jimmc414/onefilellm`
4. `AlexxIT/YandexStation`
5. `GitGuardian/ggshield`
6. `Vchitect/Latte`
7. `linbailo/zyqinglong`
8. `vacanza/holidays`
9. `anymail/django-anymail`

### Go

1. `fwdcloudsec/granted`
2. `sourcegraph/zoekt`
3. `aws/aws-node-termination-handler`
4. `ccxt/go-binance`
5. `open-telemetry/opentelemetry-operator`
6. `google/go-jsonnet`
7. `NVIDIA/dcgm-exporter`
8. `Consensys/gnark`

## Required procedure

1. Inspect the repository's current issue templates and contribution/security
   policies before contact. Do not use a security-reporting channel.
2. Use `ATTESTATION_REQUEST.v2.md` without weakening its direct-knowledge,
   exact-commit, or publication requirements.
3. Make no more than one initial public request per repository and no more than
   one follow-up.
4. Record declines, uncertainty, ineligible statements, and nonresponse as
   response states, never as human or agent labels.
5. Stop soliciting once the preregistered modeling and held-out validation
   quotas are securely met, unless extra responses are already in flight.
6. Before fitting, deduplicate exact content hashes against targets, legacy
   references, and other accepted groups. Shared hunks are excluded; repository
   roles and validation assignments remain disjoint.

Public outreach requires explicit authorization from the authenticated account
holder.

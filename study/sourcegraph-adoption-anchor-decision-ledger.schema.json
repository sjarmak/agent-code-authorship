{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "sourcegraph-adoption-anchor-decision-ledger.schema.json",
  "title": "Sourcegraph explicit-anchor decision ledger",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "$schema",
    "anchor_completion_ledger_version",
    "manifest_sha256",
    "amendment_sha256",
    "source_decision_ledger_sha256",
    "tranche_id",
    "tranche_number",
    "decision_count",
    "decisions",
    "prehistory",
    "outcomes_consulted",
    "anchor_completion_ledger_sha256"
  ],
  "properties": {
    "$schema": {
      "const": "sourcegraph-adoption-anchor-decision-ledger.schema.json"
    },
    "anchor_completion_ledger_version": {
      "const": 1
    },
    "manifest_sha256": {
      "$ref": "#/$defs/sha256"
    },
    "amendment_sha256": {
      "$ref": "#/$defs/sha256"
    },
    "source_decision_ledger_sha256": {
      "$ref": "#/$defs/sha256"
    },
    "tranche_id": {
      "$ref": "#/$defs/sha256"
    },
    "tranche_number": {
      "type": "integer",
      "minimum": 1
    },
    "decision_count": {
      "type": "integer",
      "minimum": 0
    },
    "decisions": {
      "type": "array",
      "items": {
        "$ref": "sourcegraph-adoption-review-response.schema.json"
      }
    },
    "prehistory": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "task_id",
          "earliest_unresolved_position",
          "unresolved_earlier_candidate_count",
          "clean_prehistory"
        ],
        "properties": {
          "task_id": {
            "$ref": "#/$defs/sha256"
          },
          "earliest_unresolved_position": {
            "type": "integer",
            "minimum": 1
          },
          "unresolved_earlier_candidate_count": {
            "type": "integer",
            "minimum": 0
          },
          "clean_prehistory": {
            "type": "boolean"
          }
        }
      }
    },
    "outcomes_consulted": {
      "const": false
    },
    "anchor_completion_ledger_sha256": {
      "$ref": "#/$defs/sha256"
    }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    }
  }
}
